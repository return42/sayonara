PROJECT( say_helper_playlist )
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)


SET(SOURCES
    ./CustomPlaylist.cpp
)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${SOURCES})
TARGET_LINK_LIBRARIES(${PROJECT_NAME}
#edit me
)
