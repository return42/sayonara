PROJECT( say_gui_helper_delegate )
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)


SET(SOURCES
    ./TreeDelegate.cpp
)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${SOURCES})
TARGET_LINK_LIBRARIES(${PROJECT_NAME}
    Qt5::Gui
)
