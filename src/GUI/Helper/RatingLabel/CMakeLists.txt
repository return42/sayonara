PROJECT( say_gui_rating_label )
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)


SET(SOURCES
    ./RatingLabel.cpp
)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${SOURCES})
TARGET_LINK_LIBRARIES(${PROJECT_NAME}
#edit me
)
