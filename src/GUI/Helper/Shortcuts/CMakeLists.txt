PROJECT( say_gui_helper_shortcuts )
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES(
	${CMAKE_CURRENT_BINARY_DIR},
	${CMAKE_CURRENT_SOURCE_DIR}
)


SET(SOURCES
	./ShortcutHandler.cpp
	./RawShortcutMap.cpp
)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${SOURCES})
TARGET_LINK_LIBRARIES(${PROJECT_NAME}
		Qt5::Gui
		say_helper
)
