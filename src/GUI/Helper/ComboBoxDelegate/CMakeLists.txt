CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(say_gui_combobox_delegate)

SET(SOURCES
	ComboBoxDelegate.cpp
)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${SOURCES})
TARGET_LINK_LIBRARIES(${PROJECT_NAME}
	Qt5::Widgets
)
