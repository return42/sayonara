PROJECT( say_gui_fontconfig )
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)


SET(SOURCES
	GUI_FontConfig.cpp
)


QT5_WRAP_UI(FORMS_UIC
	GUI_FontConfig.ui
)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${SOURCES} ${FORMS_UIC} )
TARGET_LINK_LIBRARIES(${PROJECT_NAME}
	Qt5::Gui
)
